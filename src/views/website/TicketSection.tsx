import TicketTabs from "../../components/tickets";

const TicketSection = () => {
  return (
    <section id="ticket" className="container">
      <TicketTabs />
    </section>
  );
};

export default TicketSection;
